//- SmallJia: keep a single coherent loading experience.
//- Source 2 remains available for Pace-only mode; all other modes use the custom fullpage loader.
if theme.preloader.source === 2
  include ./pace.pug
else
  include ./fullpage-loading.pug
